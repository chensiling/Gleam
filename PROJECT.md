# 项目总纲

> 本文件是所有 AGENT 的总纲：只做服务于"目标"的事，验证以"验证标准"为准。
> 本文件不写技术细节和编码规范——怎么做是实现自由，做什么和怎么算做完以本文件为准。

## 项目目标

打造一个**无界面的 Windows 调试器**，以静态库形态嵌入最终 exe，供 LLM 通过 MCP 协议驱动，服务于逆向分析场景。

类比：idalib 之于 IDA——调试引擎以库的形式存在，没有 UI，完全由代码驱动。

## 范围

### 要做

- 基于开源调试引擎 **GleeBug**（MIT 协议，x64dbg 官方引擎）构建调试核心
- 静态链接进最终产物，单一 exe、无外部 DLL 依赖（含 CRT）
- 命令驱动的调试会话：启动/附加进程、断点（软件/硬件/内存）、单步、继续、读写寄存器与内存、枚举模块与线程
- MCP server 封装，使 LLM 客户端能完成完整调试任务

### 不做（现阶段明确排除）

- 任何形式的图形界面
- 内核调试、dump 分析、TTD（时间旅行调试）
- 跨平台（仅 Windows）
- TitanEngine 兼容层（GleeBug 仓库里的 StaticEngine / TitanEngineEmulator 仅为上游附属物，不是本项目目标）

## 里程碑与验证标准

### M1：引擎可用（已完成）

**目标**：GleeBug 在本机 VS2022 环境下可编译，静态库形态确认可用。

**验证**：

- `GleeBug.sln` 全量编译通过（Debug|x64），产出 `GleeBug.lib`
- 一个最小 exe 静态链接 GleeBug，启动目标进程（如 `whoami.exe`），能收到进程创建、DLL 加载、进程退出事件并正常退出

### M2：命令驱动调试会话（已完成）

**目标**：调试循环可交互——不是一次跑到底，而是"执行命令 → 等待事件 → 读取状态 → 再执行"。

**验证**：对一个目标程序（自编的含已知指令序列的测试程序）完成以下序列且结果正确：

- 在指定地址设断点并命中，命中时读出的寄存器值与预期一致
- 单步执行后指令指针按预期移动
- 读取指定内存，内容与预期一致
- 修改寄存器/内存后继续运行，目标程序行为发生对应改变

**实际产出（超出原标准）**：原子功能层已覆盖三类断点、条件断点/追踪点、条件跟踪（tgo）、符号（imports/exports/反查/符号地址）、事件暂停开关（breakon）、反反调试（hide）、线程控制、内存管理、代码扫描（xref/findasm）、统一 key=value 停止记录。测试：`run_tests.sh` 23 场景 75 项断言全过。

### M3：MCP 封装

**目标**：调试能力通过 MCP 协议暴露，LLM 可驱动。

**验证**：用真实 LLM 客户端连接 MCP server，自然语言下达任务（示例："调试 X 程序，在函数 Y 入口下断，命中后告诉我 RAX 的值"），LLM 能自主调用工具完成并给出正确答案。

## 技术选型（已确定，勿推翻）

- 引擎：**GleeBug**（不用 TitanEngine——上游正在淘汰它；不用 RAD Debugger——它是完整 GUI 应用不可嵌入）
- 构建：Visual Studio 2022，v143 工具集
- 链接：静态库 + 静态 CRT（/MT、/MTd），最终 exe 自包含
